# Defaults file for pandoc "arxiv / plain" template

resource-path:
  - .
  - ${.}
  - ${USERDATA}

template: ${.}/plain/article.tex

metadata-file:
  - ./paper-meta.yaml

filters:
  - ${USERDATA}/filters/git-meta.lua
