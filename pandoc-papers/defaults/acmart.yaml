# This is a defaults file for Pandoc, 
# that configures Pandoc to use the acmart LaTeX template for ACM articles.
template: ${.}/acmart/acmart-small.tex

filters:
  - ${.}/../filters/git-meta.lua

resource-path:
  - .             # the working directory from which pandoc is run
  - ${.}/acmart   # class and images for acmart

metadata:
  # Default metadata values
  draft:     true
  anonymous: false
  final:     false

# By default, process paper.md with paper-meta.yaml
input-file: paper.md
metadata-file: paper-meta.yaml

# Writer settings
to: latex
